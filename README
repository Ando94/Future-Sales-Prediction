Future Sales Prediction

This project implements a machine learning system for detecting future sales prediction

The program needs to predict future sales using already having sales dataset so we use Linear Regression for predincting dependent variables from independent ones